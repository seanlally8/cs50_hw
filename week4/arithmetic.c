#include <cs50.h>
#include <stdio.h>
#include <string.h>

//di
int main(void)
{
    int divid = get_int("Input dividend: ");
    int divis = get_int("Input divisor: ");
    
    int field[divid];
    
    int count = 0;
    int solution[divid];
    
    for (int i = 0; i < divid; i++)
    {
        for (int m = 0; m < divis; m++)
        {
            count++;
            if (count == 2)
            {
                
            }
        }
    }
}