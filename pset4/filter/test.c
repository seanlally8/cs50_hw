#include "helpers.h"
#include <math.h>
#include <stdint.h>
#include <stdio.h>

int main(void)
{
    float avg;
    float count = 0;
    count = count + 3;
    avg = 223 / count;
    printf("%f and %f", avg, count);
}